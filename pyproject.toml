[project]
name = "langchain-agentic-ai-scaffolding"
version = "0.1.0"
description = "Nine hands-on lessons for building agents with LangChain's create_agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.4.0",
    "langchain-anthropic>=1.7.2",
    "langchain-mcp-adapters>=0.3.2",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]
